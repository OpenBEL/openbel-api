namespace:
  extension: 'rdf_backed'
  storage: 
    extension: 'redland/redland'
    options:
      name: 'rdf.db'
      storage: 'sqlite'
      synchronous: 'off'
# vim: ts=2 sts=2 sw=2
# encoding: utf-8
